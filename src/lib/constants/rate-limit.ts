/** Rate limit time window (milliseconds) - 5 minutes / 限流时间窗口(毫秒) - 5分钟 */
export const RATE_LIMIT_WINDOW_MS = 5 * 60 * 1000;

/** Rate limit maximum requests / 限流最大请求数 */
export const RATE_LIMIT_MAX_REQUESTS = 300;
